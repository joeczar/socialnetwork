import React from "react";
import Layout from "../layouts/layout";
import Logo from "../components/logo";

const Home = () => {
    return (
        <Layout>
            <Logo />
        </Layout>
    );
};
export default Home;
