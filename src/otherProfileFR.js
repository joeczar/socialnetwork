import React, { useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";
import {} from "../helpers/actions";

export const OtherProfileFR = () => {
    useEffect(() => {}, [input]);

    return (
        <div>
            <h1>OtherProfile</h1>
        </div>
    );
};

export default OtherProfileFR;
