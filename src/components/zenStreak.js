import React from "react";
import style from "../css/zenStreak.module.css";

const ZenStreak = ({ height, width }) => (
    <g id="zenStreak" className={style.zen}>
        <text>Zen Streak</text>
    </g>
);

export default ZenStreak;
